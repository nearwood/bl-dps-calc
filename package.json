{
  "name": "bl-dps-calc",
  "version": "1.2.2",
  "private": true,
  "homepage": "https://nearwood.github.io/bl-dps-calc",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "react-tabs": "^3.2.2"
  },
  "devDependencies": {
    "gh-pages": "^3.2.0"
  },
  "scripts": {
    "predeploy": "REACT_APP_GIT_COMMIT_HASH=\"$(git rev-parse HEAD)\" yarn run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
